server {
  listen 80;
  location /api/ { proxy_pass http://workspace-api:8000/; }
  location /edge/ { proxy_pass http://integration-edge:8100/; }
  location / { root /usr/share/nginx/html; try_files $uri /index.html; }
}
