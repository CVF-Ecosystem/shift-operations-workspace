FROM python:3.12-slim
WORKDIR /app
RUN pip install --no-cache-dir fastapi uvicorn[standard]
COPY apps/integration-edge/src /app/src
ENV PYTHONPATH=/app/src
CMD ["uvicorn","integration_edge.main:app","--host","0.0.0.0","--port","8100"]
