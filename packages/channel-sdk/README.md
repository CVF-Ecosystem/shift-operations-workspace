# channel-sdk

Provider-neutral closed contracts for service assertions and digest-only
outbound delivery. Delivery request/result models are immutable and adapters
expose a narrow typed port. Concrete transports and provider formats live
outside this package.
