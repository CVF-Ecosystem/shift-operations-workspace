# Customer Router

Module boundary `packages/conversation-routing/customer-router`. Any future
implementation must follow locked contracts, CVF policies, and dependency
direction.

P4-E v1 excludes this documentation-only scaffold. It must remain free of
runtime code, dependencies, configuration, schema, and composition changes.
