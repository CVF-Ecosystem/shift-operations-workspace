[project]
name = "conversation-routing"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["pydantic==2.10.6", "identity-mapping"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/conversation_routing"]
