"""operations-domain: canonical domain language for the shift operations workspace.

Submodules are imported explicitly (`from operations_domain.models import ...`,
`from operations_domain import models`) rather than re-exported here, matching
how `cvf_runtime` and `operations_ledger` are consumed in this repository.
"""
