[project]
name = "identity-mapping"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["pydantic==2.10.6", "channel-sdk"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/identity_mapping"]
