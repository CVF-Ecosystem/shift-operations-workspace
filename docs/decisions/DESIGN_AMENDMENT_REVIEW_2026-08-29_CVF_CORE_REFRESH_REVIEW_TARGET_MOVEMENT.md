# Independent DESIGN Amendment Review — Reviewer-Time Target Movement

- Tranche: `CVF-CORE-REFRESH-2026-08-29`
- Review date: `2026-08-29`
- Role: `INDEPENDENT_DESIGN_AMENDMENT_REVIEWER`
- Risk: `R2`
- Reviewed amendment raw SHA-256:
  `5e1afbc07e99ddea1f5253dbde78b24d90ca49eeff71e805079063b4f831df99`
- Accepted parent DESIGN raw SHA-256:
  `2e383c0918a77d3262b9a065e8cbeca5a4e5798dfd7e4771c311f4f0af049443`
- Trigger: `CORE-REFRESH-SPEC-REV-F4`
- Disposition: `DESIGN_AMENDMENT_REVIEW_CHANGES_REQUIRED`

## Review boundary

This review rehydrated current canonical continuity, then used only
allowlisted local reads and deterministic guards. Its sole mutation is this
reviewer-owned artifact. It performed no network or doctor call, reconciler or
initializer execution, hidden-Core/workspace-root/product/continuity mutation,
provider call, credential use, installation, database action, deployment,
commit or push. It did not touch, read, hash or inventory the protected
operator assessment and performed no broad untracked inventory.

## Independently verified amendment properties

1. The amendment preserves the accepted old pin, frozen BUILD target, original
   three worker outcomes and narrow deterministic reconciliation claim. It
   grants no reconciliation retry, target adoption, product/provider effect or
   current external-effect authority.
2. The original root-effects receipt, worker return and target-movement
   completion review remain immutable. Later public-target movement does not
   rewrite the truthful `SUCCESS` state observed at worker return.
3. The conditional rollback JSON has an exact path and owner. BUILD preflight
   must prove it `ABSENT`; it remains absent for normal review and every
   initial-worker failure; only the routed distinct `REPAIR_WORKER` may create
   it once after `REVIEW_TARGET_MOVEMENT`. It is the sole semantic owner of the
   conditional rollback attempt and does not self-hash.
4. The terminal rereview receipt also has an exact path and owner. BUILD
   preflight must prove it `ABSENT`; only a fresh
   `INDEPENDENT_COMPLETION_REREVIEWER` may create it after the rollback record
   exists. It records final hashes of the four earlier immutable artifacts and
   does not self-hash. This closes the hash lifecycle without a self-hash or
   cross-hash cycle.
5. Complete and incomplete rollback are distinct terminal outcomes. The
   incomplete outcome records only observed states, forbids a clean-restoration
   claim and cannot authorize retry or adoption of a newer target.
6. The existing invariant family remains the sole outcome-semantic owner. The
   amendment correctly routes its two new conditional outcomes and exact
   path/effect relations to matrix amendment during SPEC repair instead of
   copying a second outcome matrix into DESIGN prose.

## Blocking finding

1. **`CORE-REFRESH-DESIGN-AMEND-REV-F1` — the claimed non-overlapping role
   ceilings contradict the declared repair path set.** Amendment section 7
   requires review to verify “non-overlapping initial-worker, repair-worker and
   reviewer ceilings.” Section 4, however, authorizes the repair worker to
   restore or update the same two pin carriers, nine shared-continuity carriers
   and ignored binding already inside the initial-worker ceiling. Those twelve
   carrier paths/effects are therefore intentionally overlapping even though
   the worker evidence, conditional rollback and reviewer receipt paths are
   disjoint. The current text does not define this as a closed, temporal
   ownership handoff, so the literal acceptance condition cannot be satisfied
   and a later Work Order cannot state an exact ceiling without interpreting
   the DESIGN.

   Repair the amendment by replacing the impossible blanket
   “non-overlapping ceilings” requirement with an exact role-and-state matrix:
   enumerate the intentional initial-worker/repair-worker intersection; limit
   repair ownership of that intersection to the post-
   `REVIEW_TARGET_MOVEMENT` rollback window and restoration/failure-continuity
   purposes; keep all three immutable earlier evidence artifacts forbidden to
   the repair worker; and keep the conditional rollback JSON and terminal
   rereview receipt disjoint with their existing exact owners and `CREATE`
   lifecycle. Alternatively, redesign the path owners so the ceilings truly
   are path-disjoint. Do not widen the existing target, command, network,
   product, provider or commit authority.

## Waivers

`NONE`.

## Deterministic local guards

- Amendment reviewer artifact pre-state: `ABSENT`.
- Session-state/mirror guard: `PASS`.
- Project Knowledge guard: `PASS`.
- Invariant-family repository guard: `PASS` with no diagnostics.
- Catalog drift guard: `PASS` (`26` modules).
- File-size guard: `PASS`.
- Scoped amendment diff check before review receipt: `PASS`.
- Staged set: empty.

## Final disposition

`DESIGN_AMENDMENT_REVIEW_CHANGES_REQUIRED` — finding
`CORE-REFRESH-DESIGN-AMEND-REV-F1` is open; waivers `NONE`.

Return only the ceiling-ownership contradiction to a declared `REPAIR_WORKER`.
After bounded repair, a fresh independent amendment rereview must recompute the
amendment hash and close or retain F1. SPEC repair, Work Order,
reconciliation/network/root effects, P4-E SPEC, commit and push remain
unauthorized.

---

## Independent DESIGN amendment rereview — F1 closure — 2026-08-29

- Role: `INDEPENDENT_DESIGN_AMENDMENT_REREVIEWER`
- Recomputed repaired amendment raw SHA-256:
  `2f250e98914f671b19f7be3a820f2b216c277e8f8900e2f77ceaab69255a44e0`
- Finding `CORE-REFRESH-DESIGN-AMEND-REV-F1`: `CLOSED`
- Adjacent findings: `NONE`
- Waivers: `NONE`
- Disposition: `DESIGN_AMENDMENT_REVIEW_PASS`

### Rereview boundary

This fresh rereview rehydrated current canonical continuity and then used only
allowlisted local reads and deterministic guards. Its sole mutation is this
appended reviewer-owned section. It performed no network or doctor call,
reconciler or initializer execution, hidden-Core/workspace-root/product/
continuity mutation, provider call, credential use, installation, database
action, deployment, commit or push. It did not touch, read, hash or inventory
the protected operator assessment and performed no broad untracked inventory.

### Closure of `CORE-REFRESH-DESIGN-AMEND-REV-F1`

`CLOSED`. The repaired amendment replaces the contradictory blanket
path-disjointness condition with an exact role-and-state ownership matrix:

1. The intentional intersection is closed to exactly the two pin carriers,
   nine shared-continuity carriers and ignored local binding already declared
   by the accepted DESIGN.
2. Repair ownership begins only after the immutable completion review records
   `REVIEW_TARGET_MOVEMENT` and routes the distinct repair worker. It ends when
   that worker writes its one terminal rollback JSON. Initial-worker and
   reviewer mutation ownership are inactive throughout that window.
3. The intersection permits only pin/binding restoration to BUILD preimages
   and shared-carrier restoration followed by truthful rollback continuity. It
   grants no new purpose, content class, reconciliation retry or target
   adoption.
4. The two original worker evidence artifacts and target-movement completion
   review remain immutable/read-only during repair.
5. The conditional rollback JSON remains repair-worker-only `ABSENT → CREATE`;
   the conditional terminal rereview remains rereviewer-only
   `ABSENT → CREATE`. The original evidence paths, conditional rollback path
   and terminal rereview path therefore remain disjoint and owner-only.

### Adjacent-risk result

No adjacent defect was identified. The complete/incomplete rollback outcomes
remain honest and mutually distinct; the incomplete branch records only
observed states and forbids a clean-restoration claim. The conditional rollback
JSON does not self-hash, while the later rereview records final raw hashes of
all four prior immutable artifacts and does not self-hash. No self-hash or
cross-hash cycle exists. The existing invariant family remains the sole
semantic owner and must be amended and re-pinned during SPEC repair. Target,
network, product, provider, commit and current external-effect authority are
not widened.

### Deterministic local guards

- Session-state/mirror guard: `PASS`.
- Project Knowledge guard: `PASS`.
- Invariant-family repository guard: `PASS` with no diagnostics.
- Catalog drift guard: `PASS` (`26` modules).
- File-size guard: `PASS`.
- Scoped amendment/review diff check before this append: `PASS`.
- Staged set: empty.

### Final rereview disposition

`DESIGN_AMENDMENT_REVIEW_PASS` — F1 is closed; findings `NONE`, waivers
`NONE`.

The ORCHESTRATOR may resume bounded SPEC/matrix/pin repair for the previously
returned SPEC F1-F4 only. Work Order, reconciliation/network/root effects,
P4-E SPEC, commit and push remain unauthorized.
