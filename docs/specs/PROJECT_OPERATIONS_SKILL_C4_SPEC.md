# PROJECT-OPERATIONS-SKILL C4 SPEC

- Parent BUILD: `ad7e0375789f3273c68027ddfa56f80ac3923a22`
- Risk: `R2`
- Status: `SPEC_COMPLETE_PENDING_INDEPENDENT_AUTHORIZATION`

## Requirements

- `R1` Change exactly the eight C4 paths in the ADR; no BUILD/governance-source
  path and no ninth closure path may change.
- `R2` Preserve BUILD evidence at replacement 4 `4/4` and total `12/8/4`;
  make zero provider calls and retain no runtime residue.
- `R3` Set the control-chain disposition to `FREEZE / CLOSED_BOUNDED`, record
  BUILD `ad7e037`, independent final review, focused/full/repository/doctor and
  rollback evidence, and keep the bounded claim exact.
- `R4` Synchronize canonical memory, canonical active state, compatibility
  mirror and active handoff without drift.
- `R5` Update implementation truth and the five-phase roadmap from stale
  Amendment 1 state to final Amendment 4 closure.
- `R6` Add the repository-owned `operate-shift-workspace` skill to the module
  registry as guidance/contract evidence, not runtime enforcement; regenerate
  catalog metrics and Markdown mechanically.
- `R7` Activate only fresh `PROJECT-KNOWLEDGE-PACK` INTAKE. Refinery,
  retrieval, RAG and learning remain parked with no BUILD/provider authority.
- `R8` Keep installation, production-governance, universal-model-compliance
  and prompt-enforcement claims explicitly outside closure.

## Acceptance criteria

- `AC-1` Exact eight-path diff and zero staged/outside/runtime residue.
- `AC-2` BUILD commit and evidence bundle hashes remain unchanged.
- `AC-3` JSON, session mirror, catalog, file-size and repository validators PASS.
- `AC-4` Generated catalog is current and records the skill without an
  `enforced` status or load-bearing claim.
- `AC-5` Focused 76 and full 1454/128 remain PASS; doctor remains PASS with only
  the bounded legacy catalog-kit note.
- `AC-6` Independent C4 review returns `FREEZE_REVIEW_PASS` without waiver.
- `AC-7` C4 is committed/pushed separately and clean `HEAD == origin/main`.

## Stop conditions

Stop on path overflow, BUILD/evidence mutation, continuity drift, failed gate,
catalog hand-edit, broadened claim, provider-call possibility, nonzero residue,
missing independent review or unapproved installation.

