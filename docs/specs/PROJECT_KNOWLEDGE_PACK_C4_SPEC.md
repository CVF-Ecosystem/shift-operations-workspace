# PROJECT KNOWLEDGE PACK C4 SPEC

- Parent BUILD: `bb3e33668a6d60585455bf0301ba059918a15890`
- Risk: `R2`
- Status: `SPEC_COMPLETE_PENDING_INDEPENDENT_AUTHORIZATION`

## Requirements

- `R1` Change exactly the eight C4 paths in the ADR; no BUILD, C4-authority or
  ninth closure path may change.
- `R2` Preserve exact BUILD bytes and record independent
  `FINAL_REVIEW_PASS`, F1-F4 closed without waiver, focused `86`, full
  `1540/128`, repository/doctor/cleanup evidence and zero provider/network/POST.
- `R3` Set Project Knowledge Pack to `FREEZE / CLOSED_BOUNDED` under the exact
  local validation/disposable-helper claim.
- `R4` Synchronize canonical memory/state, mirror and active handoff without
  drift; implementation status and roadmap must agree on closure and next move.
- `R5` Add exact `project-knowledge-pack` registry entry with `partial` status,
  no load-bearing CVF control and explicit local-only enforcement boundary;
  regenerate catalog Markdown mechanically.
- `R6` Activate only fresh `P3-A REFINERY INTAKE ONLY`; all subsequent queue
  items remain parked and unimplemented.
- `R7` Make zero provider/helper/network/POST/external calls and retain no
  `_index.json` or temporary residue.
- `R8` Do not claim remote ingest, retrieval, automatic injection,
  provider/model behavior, DLP/minimization, Refinery enforcement, RAG,
  learning memory, OS-level zero-packet proof or production readiness.

## Acceptance criteria

- `AC-1` Exact eight-path unstaged closure candidate; protected hashes match
  the `bb3e336` tree and pushed C4 authority commit.
- `AC-2` JSON, session mirror, catalog, file-size, repository and diff checks
  PASS; generated catalog is current.
- `AC-3` `python scripts/check_project_knowledge.py` and focused `86` PASS;
  full non-live `1540/128` remains accurately recorded.
- `AC-4` Workspace doctor PASS with only the bounded legacy warning.
- `AC-5` Independent C4 reviewer returns `FREEZE_REVIEW_PASS`, no waiver.
- `AC-6` C4 closure is committed/pushed separately; clean
  `HEAD == origin/main`, zero staged/runtime residue.

## Stop conditions

Stop on missing R2 approval, path overflow, BUILD/authority hash drift,
continuity/catalog disagreement, catalog hand edit, failed gate, provider or
helper execution, network/POST/external-write path, residue, broadened claim,
unparked later work or missing independent review.
