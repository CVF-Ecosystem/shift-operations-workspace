# Work Order Amendment 4 — Project Operations Skill Phase Semantics Repair

- Parent: base Work Order + Amendments 1–3
- Risk: `R2`
- Status: `AMENDMENT_4_AUTHORIZATION_RE_REVIEW_PASS — GOVERNANCE PUSH THEN SEPARATE RESUME/G6-R4 REQUIRED`

## 1. Exact boundaries

The final BUILD set remains exactly the same eight paths: the skill Markdown,
OpenAI metadata, skill contract test, runner, support module, live-evidence
test, receipt and state. Only runner/support/live test/receipt/state are
expected to change; the first three receive no-op inspection. Runtime-only
paths remain exactly the existing lock, state `.tmp` and receipt `.tmp`. No
ninth path, install, catalog/provider configuration or new runtime file exists.

## 2. G6-R4 checkpoint

Before BUILD, verify receipt `80883/5ab461564c942785806354019ee5f7f795872672ff9d63261d221a5cad9d5cc3`
and state `394267/3a8d6f66477939631b9a6bc0f32e0da2eacdd0c0e295e427f2e00e25cc3c85db`;
replacement3 FT-1 ACCEPTED/1, FT-2 FAILED/1, FT-3..FT-4 UNUSED/0; physical 8,
governance accepted 0; nested evidence exact; HEAD=origin, staged/residue zero,
and the same eight untracked BUILD paths.

## 3. Authorized order after pushed resume

1. Apply SPEC D1's global phase definition and exhaustive tests.
2. Introduce v5 with exact v4 snapshot/prefix, recursive older preservation,
   immutable replacement3 and fresh `replacement_4_final` lineages computed
   from exact `replacement4|FT|bundle|fixture` UTF-8 input.
3. Enforce exact 8/8/0 migration, 12/8/4 final accounting, no older dispatch,
   retry or thirteenth call; retain all prior safety/failure tests.
4. Run focused/full/skill/repository/diff/secret/doctor gates.
5. Obtain independent source/temp-migration review; migrate repo once with zero
   calls; repeat gates and obtain migrated-state pre-call review.
6. Obtain a fresh human R2 acknowledgment, then invoke the runner once for at
   most four calls. Stop first failure; never rerun.
7. Run post-call gates, exact-parent rehearsal and independent final review.
   Only REVIEW_PASS transfers eight-path commit ownership.

## 4. Roles and commits

After independent authorization PASS, SESSION_SYNC_STEWARD commits/pushes
exactly: the Amendment 4 ADR, SPEC and Work Order plus SESSION memory, canonical
active state, compatibility mirror and active handoff. No BUILD path enters
that seven-path commit. A separate commit/push changes exactly the four
continuity paths and transfers BUILD/REPAIR_WORKER with G6-R4 first. Reviewer
stays independent; C4/later queue items remain separate.

## 5. Stop conditions

Stop on pin/path/history drift, lost recursive evidence, answer leakage,
retroactive FT-2 acceptance, unsafe candidate, old-set dispatch, mixed bundle,
rollback, preflight mutation/call, failed gate, secret/envelope/residue,
replacement failure, retry/thirteenth-call possibility, missing review or
missing fresh human R2 acknowledgment.

Authorization re-review passed without finding or waiver. Commit/push exactly
the seven governance paths, then the separate four-path resume. No BUILD edit,
migration or provider call exists before pushed resume and G6-R4.
