# Active Handoff — CVF Public-Core Refresh

- Tranche: `CVF-CORE-REFRESH-2026-08-23`
- Date: `2026-08-23`
- Risk: `R2`
- Phase: `FREEZE`
- Status: `CLOSED_BOUNDED`
- Active role: `CLOSER`

## Authority acknowledgment

After the consumed evidence-contract rereview, the operator directed: `xử lý
đi, cái cần là tập trung cho project đang dang dở`, following the explicit
instruction not to over-engineer low-value quota/latency controls. This grants
fresh authority to simplify the acceptance contract around the sanctioned
bootstrap path and progress through the remaining phase gates. External effects
remain gated until authorization review PASS.

After completion review retained F1 only, the operator explicitly answered
`đồng ý amendment F1`. This authorizes a minimal DESIGN/SPEC/Work Order
acceptance amendment replacing only the unavailable 33 non-carrier byte-
preimage predicate with existing frozen path/status and command/path-bound
changed-set proof. It authorizes no BUILD or doctor rerun, commit or push.

After the recorded SPEC amendment PASS, the operator answered `tiếp tục` at
the explicit next move. This authorizes only a bounded Work Order amendment
that consumes the pinned evidence matrix and its independent authorization
review. It grants no reconciliation, network, install, BUILD, commit or push.

The operator explicitly approved: `đồng ý mở DESIGN/SPEC amendment cho
evidence contract`. This reopens the control chain at DESIGN for only the
three provenance blockers retained by the consolidated authorization
rereview. SPEC may begin only after independent DESIGN amendment PASS. It
grants no Work Order repair, reconciliation, network, install, BUILD, commit
or push authority.

After the third authorization rereview recorded
`REVIEW_COST_ESCALATION_REQUIRED`, the operator answered `đồng ý` to the
explicit request for exactly one consolidated repair/rereview cycle covering
only the three named residuals. This acknowledgment grants no reconciliation,
BUILD, network, install, commit or push authority.

Workspace-doctor freshness blocked the independent rereview of the repaired
P4-C Work Order. The operator answered `next` to the explicit request for
authority to reconcile Core and return to P4-C. This opens only the Core
refresh INTAKE; later phases do not inherit authority.

After the first bootstrap-native BUILD attempt stopped and rolled back on
target movement, the operator explicitly approved target `864c4e0`. This opens
only a minimal gated rebase of the frozen target and dependent pins; all other
scope, rollback and external-effect boundaries remain unchanged.

The target-rebase INTAKE, DESIGN and SPEC amendments independently passed
`NONE/NONE`. Work Order raw `aee8159b...` then received
`AUTHORIZATION_REVIEW_PASS`, `NONE/NONE`; exact BUILD/network authority for
target `864c4e0...` is active for the separate implementation worker. The
non-assessment porcelain baseline remains the same 39 paths with canonical LF
digest `f2e54fee...`. No commit or push is authorized.

## Current boundary

Canonical INTAKE:
`docs/decisions/INTAKE_2026-08-23_CVF_CORE_REFRESH.md`.

Local/manifest Core is `7d9f360a...`; fetched public target is
`3b031fec35473e6ee6a554c4c72400e7a23b06c5`. Current Core is clean and an
ancestor of the target. The upstream delta is documentation-only, but the
sanctioned reconciler has R2 workspace-root, backup and public-network effects
that require exact reviewed authority and rollback evidence.

No reconciliation, pin edit, root write, provider call, credential use,
install, deployment, commit or push has occurred in this tranche.

Independent INTAKE review returned `INTAKE_REVIEW_CHANGES_REQUIRED`, findings
`CORE-REFRESH-INTAKE-REV-F1..F2`, waivers `NONE`. Both were accepted and
repaired in one pass: the inventory now covers all 17 root targets, including
the baseline and three deletion candidates; and the non-atomic reconciler gap
now requires a containment-checked, hash-verified, preservation-only rollback
for Core, root and downstream preimages. No later-phase action occurred.

Bounded INTAKE rereview returned final `INTAKE_REVIEW_PASS`; F1-F2 closed and
findings/waivers are `NONE/NONE`. `DESIGN_AUTHOR` then selected the sanctioned
backup-and-reclone path, exact 17-root/12-downstream incremental ceilings,
initial frozen public Git boundary, dirty-worktree preimage accounting,
four-way pin equality and executable non-atomic rollback architecture.

Independent DESIGN review accepted every other boundary but returned
`CORE-REFRESH-DESIGN-REV-F1`, waivers `NONE`: initializer invokes doctor, so
the selected BUILD graph performs three rather than two Git operations. The
repair now freezes exactly three worker-owned operations (clone, initializer
fetch, initializer-doctor fetch) plus one separately owner-scoped doctor fetch
per independent review/rereview, each bound to the exact public URL and frozen
target with no credentials and rollback/stop on target movement.

Bounded DESIGN rereview returned final `DESIGN_REVIEW_PASS`, finding/waiver
`NONE/NONE`. `SPEC_AUTHOR` converted the accepted design into R1-R17 and
AC-01..05, including exact 17-root/12-path/3-BUILD-operation evidence,
incremental dirty-worktree proof, pin/knowledge/continuity equality and
preservation-first rollback.

Independent SPEC review returned `SPEC_REVIEW_CHANGES_REQUIRED`, F1-F3,
waivers `NONE`. Repair retains safety rules on success/failure, distinguishes
the successful three-operation path from failure prefixes and one conditional
rollback-verifier fetch, and defines five deterministic acceptance validators.
Because the conditional fourth fetch corrects accepted DESIGN, its amendment
requires bounded independent validation before SPEC rereview.

Independent DESIGN amendment review returned `AMENDMENT_PASS`, `NONE/NONE`.
SPEC rereview closed F2-F3 without waiver and returned only residual
`CORE-REFRESH-SPEC-REV-F1-R1`. Repair round 2 now retains R1-R5, R7-R12 and
R14-R17 on failure; only refreshed-target equality and success gates remain
success-only.

Final bounded SPEC rereview returned `SPEC_REVIEW_PASS`; F1-F3 and residual
F1-R1 are closed without waiver, findings/waivers `NONE/NONE`.

`WORK_ORDER_AUTHOR` then authored the exact 12-path downstream increment,
17-root effect ceiling, protected 26-path baseline, three-success/four-failure
network accounting, validator predicates and executable rollback contract.
Its original reviewed raw-byte SHA-256 was
`969782ddf5034ed952eb5478dfe65ce01e4e87d7fec1f1c9da8f175ac098791e`;
the first repaired Work Order raw-byte SHA-256 was
`44a2c88f09ae11923fbddd09cc56226ad5c3f1e3fbdf373bcb70a0d560e13da0`.
BUILD authority remains `NOT GRANTED`.

Independent authorization review returned `CHANGES_REQUIRED`, F1-F4,
waivers `NONE`. The repair removes the unreproducible historical content
digest in favor of BUILD-start preimages, binds frozen arrays and executable
validator code, adds non-credentialed trace2/packet raw evidence for nested Git
operations, and distinguishes zero dependency/package installs from the exact
authorized root-wrapper refresh. No external effect occurred.

Bounded rereview closed F1/F4 without waiver and returned residual F2-R1/F3-R1:
the first validator trusted receipt assertions instead of actual status,
preimages and raw evidence, and the trace contract did not parse/correlate each
operation or establish a fresh reviewer-owned environment. Repair round 2 now
binds exact 26/12/17 arrays, actual porcelain delta, recomputed current/preimage/
root/trace hashes, success/failure-specific predicates, trace `sid`/argv/exit/
packet-ref correlation, extra-session rejection, and fresh trace ownership per
review. Static validator and repository guards pass. Current repaired Work
Order raw-byte SHA-256 is
`aadb91be972953637570eed0b576d0ab1b2165703b706f58be175d44c067be02`.

Second bounded rereview retained `CHANGES_REQUIRED`, waivers `NONE`. It exposed
the independently new clean-to-dirty inventory root cause plus rollback and
trace residuals F2-R1-R1, F2-R1-R2 and F3-R1-R1. Repair round 3 now derives the
return set from four exact initially-clean carriers, enforces failure-carrier
restoration and preserved-delta inventories, tolerates the old Core backup's
required move-back, requires complete command exits, clears trace variables in
parent and child, rehashes raw evidence after all subprocesses, separates the
rollback pair, orders indistinguishable fetch spans by raw start time, and
prevents reviewer operation-id reuse. This is a new-root repair, so
`REVIEW_COST_ESCALATION_REQUIRED` is not triggered. Current Work Order raw-byte
SHA-256 is
`2a9092ddc83b6a29b751599327f2c902a9d40c90beb2b0d69d5c9f2014d869ad`.

Third bounded rereview retained `CHANGES_REQUIRED`, waivers `NONE`, for three
same-root residuals: freeze all ten carrier byte preimages and restore all ten
on failure; derive replacement-Core preservation and command lineage from the
observed network prefix; and bind worker/reviewer trace timestamps to fresh
execution windows with prior-review replay rejection. It recorded
`REVIEW_COST_ESCALATION_REQUIRED`. No fourth repair is authorized.

The operator-authorized consolidated repair now freezes/restores all ten
carrier byte preimages, derives failed replacement and command lineage from
observed operations, and binds BUILD/REVIEW trace evidence to UUID anchors,
raw command transcripts, full start/exit windows, span fingerprints and an
append-only reviewer-anchor directory. Read-only adversarial audit and all
static/repository guards pass; the Work Order is 593 lines. Current raw-byte
SHA-256 is
`6ab0929b0d1050c6315fe27d1e18124c7e5b1867106312a7a423e4d8ea906a1a`.

The one authorized post-escalation rereview consumed that authority and
returned `AUTHORIZATION_REVIEW_CHANGES_REQUIRED`, waivers `NONE`. Accepted
boundaries include the ten-carrier contract, outcome equations, reconciler
hash/path rules, structural branch satisfiability and UUID/window/SID/span
controls. Three provenance blockers remain: immediate post-reconciler evidence
for absent/partial replacement and byte inventories for prior failed candidates;
outer PowerShell PID/command ancestry for BUILD, rollback and reviewer doctor;
and an externally frozen pre-rereview completion/anchor digest proving append-
only history. No further repair or amendment is authorized.

## Parked checkpoint

P4-C remains `WORK_ORDER_REPAIR_READY_FOR_REREVIEW`: finding
`P4C-WO-AUTH-REV-F1` is repaired, exact-66 ceiling unchanged, and BUILD is
unauthorized. Its handoff, Work Order and authorization review are protected.
The operator assessment remains untouched and evidence-ineligible.

## Next governed move

Complete the bounded evidence-contract Work Order amendment and obtain its
independent authorization review. Reconciliation, network and BUILD remain
unauthorized pending review PASS and separate operator BUILD authority.

The DESIGN amendment is now authored in
`docs/decisions/DESIGN_2026-08-23_CVF_CORE_REFRESH.md` and returned for
independent review. It selects checkpoint observation, UUID-bound deterministic
command correlation and reviewer-observed append preservation; it expressly
rejects claims of continuous absence, kernel-attested ancestry and external
immutability. It also supersedes invariant-family `NOT_APPLICABLE` for the
later SPEC amendment.

Independent DESIGN amendment review returned `AMENDMENT_PASS`, findings and
waivers `NONE/NONE`. SPEC is now active to define the testable contract and
register the mandatory invariant family. No Work Order repair or external
effect is authorized.

The SPEC amendment is now authored with AR1-AR12 and AAC-01..06. Registered
family `CVF-CORE-REFRESH-EVIDENCE-CONTRACT` owns seven terminal outcomes at
canonical digest `b7c6fae25546fed3d809ded53c8fe0c582cf0d188619044c7f4f856887ba6c39`;
the repository guard and focused invariant tests pass. It is returned for
independent SPEC amendment review.

Independent SPEC amendment review returned `SPEC_AMENDMENT_CHANGES_REQUIRED`,
waivers `NONE`: F1 requires the matrix—not SPEC prose—to own explicit candidate,
command-correlation and append-preservation relations; F2 requires a distinct
post-BUILD-start/pre-reconciler stopped outcome. Both findings are accepted for
one bounded SPEC/matrix repair and rereview.

Repair F1 makes the matrix own explicit checkpoint/preservation equality,
command-contract identities, envelope/trace UUID equality, zero bare-fetch
substitution, review pre/post equality/digests and exact counters. Repair F2
adds `PRE_RECONCILER_STOP`, separate from invoked-reconciler
`FAILURE_PREFIX_0`. The family now owns eight outcomes at canonical digest
`eecab315cc0f17c7a71ea9979b6f24d4e3ae0e778c1b3344c8fcf54e4f8e7620`;
pin, guard and focused tests pass. Returned for bounded independent rereview.

Bounded rereview closed F2 and retained F1-R1/R2 without waiver: full Core
byte-equality is infeasible across authorized Git admin changes, and
`FAILURE_PREFIX_1` must admit initializer invocation before its fetch. Both
residuals are accepted for a second bounded SPEC/matrix repair.

Repair round 2 scopes Core equality to tracked target/tree/worktree state plus
allowed Git-admin delta and admits the exact initializer-before-fetch variant
under `FAILURE_PREFIX_1`. Current matrix digest is
`b62eae333a65a6770727abed9348828ac1ca61805f5fc8c48c5fd0e41053228e`.

Independent repair-round-2 rereview returned `SPEC_AMENDMENT_PASS`; F1-R1 and
F1-R2 are closed, findings/waivers `NONE/NONE`. Both PREFIX_1 command variants
match uniquely and reject `99/99` generated mutations. The DESIGN/SPEC
amendment authority is consumed. Fresh operator authority is required before a
bounded Work Order amendment; reconciliation, network and BUILD remain
unauthorized.

The operator then answered `tiếp tục`, authorizing only the bounded Work Order
amendment and independent authorization review. The amendment is now authored
at `docs/work_orders/CVF_CORE_REFRESH_2026-08-23_EVIDENCE_CONTRACT_AMENDMENT.md`
with its frozen adapter at
`docs/work_orders/cvf_core_refresh_evidence_adapter.py`; BUILD, reconciliation
and network remain unauthorized.

Independent authorization review returned
`AUTHORIZATION_REVIEW_CHANGES_REQUIRED`, findings F1-F5 and waivers `NONE`.
The accepted single repair cycle must make the two surfaces exercise all eight
outcomes, derive command/Core/candidate/review facts from raw evidence, consume
rather than duplicate matrix semantics, and prove append state. BUILD remains
unauthorized.

The repair now routes both surfaces through one all-eight matrix-driven
derivation, removes raw outcome/expected-command inputs, validates contained
candidate/Core trees and command transcript/exit/trace2/packet joins, derives
review preservation from actual pre/post trees and binds the new run/anchor to
the reviewer command. A final pre-rereview self-audit added descendant packet
SID/PID, advertised-main parsing and command/operation exit coupling. Current
amendment/adapter/runner digests are `b42938da`/`627aa1ef`/`8367c90d`;
focused tests are `72 passed, 2 skipped`, and static guards pass. Returned for
the one bounded rereview and review-owned corpus.

The bounded rereview returned final `AUTHORIZATION_REVIEW_CHANGES_REQUIRED`,
waivers `NONE`; the one repair/rereview cycle is `CONSUMED`. Four blockers
remain: runner byte pin mismatch, honest local non-network Git spans rejected
by the closed trace schema, Git replace/graft spoofing not rejected, and no
executable replacement for retained Probe E after parent-validator
supersession. No corpus marker was created. Fresh operator direction is
required before any further amendment repair; BUILD remains unauthorized.

## Predecessor/return pointer

Bootstrap-native simplification DESIGN is authored at
`docs/decisions/DESIGN_2026-08-23_CVF_CORE_REFRESH_BOOTSTRAP_NATIVE_SIMPLIFICATION.md`.
It retires the custom 8x2 proof path in favor of minimum useful bootstrap
evidence. First review accepted that direction and returned F1-F2 only; repair
now requires verified rollback of old Core plus all `17/17` root and `10/10`
carrier preimages, and freezes the minimal success graph at three worker Git
operations plus one independent-review doctor fetch. Current DESIGN digest is
`7a6634c831013b55464d61cb32cc020b5f64eeca704dea5213e182a27aee9efa`.

Independent DESIGN rereview returned `DESIGN_REVIEW_PASS`, findings/waivers
`NONE/NONE`. The minimal bootstrap-native SPEC is now authored at
`docs/specs/CVF_CORE_REFRESH_BOOTSTRAP_NATIVE_SIMPLIFICATION_SPEC.md`; it
contains S1-S12 and AC1-AC6, retires the 8x2 execution path without replacing
it with another evidence framework. First review accepted all other clauses and
returned only F1; the repair now makes preflight refusal zero-effect/no-doctor
and reserves rollback verification for post-execution failure. Current SPEC
digest is `5946c55a4a50aa6626a3f39a08b2e5e04b1e9e87897a9cd1592a27594484e7fe`.

Independent SPEC rereview returned `SPEC_REVIEW_PASS`, findings/waivers
`NONE/NONE`. The minimal bootstrap-native Work Order is now authored at
`docs/work_orders/CVF_CORE_REFRESH_2026-08-24_BOOTSTRAP_NATIVE_WORK_ORDER.md`.
It is the complete execution contract, retains exact `17/12/10`, uses only the
sanctioned scripts and minimal direct evidence. Independent authorization
review returned `AUTHORIZATION_REVIEW_PASS`, findings/waivers `NONE/NONE`;
the exact Work Order BUILD/network authority is active for the separate worker.

The authorized worker then stopped on `TARGET_MOVEMENT`: command 1 cloned
public tip `864c4e0e6139f3e32067dea41f43f240e505c0d8` rather than frozen target
`3b031fec35473e6ee6a554c4c72400e7a23b06c5`; command 2 was not invoked. The
failed replacement was preserved and rollback independently rechecked locally:
old Core is clean at `7d9f360a...`, all `17/17` root and `10/10` carrier
preimages match, project status is restored and staged is empty. The required
rollback doctor ran once and failed only freshness because public main is now
`864c4e0...`. Evidence is under workspace-root `_cvf-core-backups/` at
`CVF_CORE_REFRESH_BUILD_20260823T172126584Z`. This is not a successful refresh;
fresh operator authority is required before rebasing the frozen target.

Return to `SESSION/handoffs/P4C_INTEGRATION_EDGE_2026-08-23.md` only after this
maintenance tranche reaches reviewed closure.

The target-rebased attempt later reached command 2 and stopped on
`INITIALIZER_PIN_REFUSAL`: reconciler cloned exact `864c4e0...`, but the
portable manifest still pinned `7d9f360a...`, so initializer correctly refused
a reverse fast-forward. Full rollback again restored clean old Core, `17/17`
roots, `10/10` carriers, the exact project status and staged zero; one rollback
doctor ran and failed only freshness. Evidence:
`_cvf-core-backups/CVF_CORE_REFRESH_REBASED_BUILD_20260823T174202004Z`.
Source inspection proves the reconciler's optional manifest updater only
matches legacy `cvfCorePath`, while this schema v2 manifest intentionally uses
`cvfCoreRelativePath`; initializer consumes but never changes the pin. The
minimal same-scope repair is an explicit local manifest/AGENTS pin sync between
the two already-sanctioned commands, followed by normal continuity/knowledge
sync. No third BUILD attempt is authorized before DESIGN/SPEC/Work Order review.

## Third BUILD worker return

The independently authorized pin-carrier Work Order at raw SHA `563cffe5...`
completed successfully. The reconciler and initializer each ran exactly once
and exited `0`; between them, one scoped local step changed only the full
`cvfCoreCommit` value in `.cvf/manifest.json` and the full `CVF Commit` header
in `AGENTS.md` from `7d9f360a...` to `864c4e0...`. The post-reconciler Core was
clean with `HEAD == origin/main == 864c4e0e6139f3e32067dea41f43f240e505c0d8`.
The initializer-owned doctor returned `PASS WITH NOTE (24 passed, 1 warning)`
and `FRESH_CLONE_CONTINUITY_PASS`. Complete old-Core, `17` root and `10`
carrier preimages plus plain transcripts are preserved at
`_cvf-core-backups/CVF_CORE_REFRESH_PIN_CARRIER_BUILD_20260823T175402170Z`.
No additional success-side doctor, provider, install, database, deployment,
commit, or push occurred.

The next governed move is a separate independent completion review of the
worker artifacts followed by its one review-owned completion doctor. P4-C
remains parked until this maintenance tranche reaches reviewed closure.

Independent completion review ran exactly one reviewer-owned doctor: exit `0`,
`PASS WITH NOTE (24 passed, 1 warning)` with only the bounded legacy catalog
warning. Core/pin/command/exit/local-pin/17-root/10-carrier/12-worker-path and
staged checks all passed. Disposition remains
`COMPLETION_REVIEW_CHANGES_REQUIRED` solely for
`CORE-REFRESH-COMPLETION-REV-F1`: preflight froze all 39 non-assessment path
statuses but captured byte preimages only for the ten carriers, so SPEC S9's
claim that all 33 preexisting non-carrier dirty paths retained exact bytes
cannot be independently proven after the fact. Resolving it by changing the
acceptance contract requires fresh operator approval; no BUILD rerun, closure,
commit or push is authorized yet.

The operator approved F1 amendment. Its DESIGN, SPEC and Work Order gates each
passed independently with findings/waivers `NONE/NONE`. Final fixed-evidence
rereview returned `COMPLETION_REVIEW_PASS`, closed F1 and made no 33-path byte-
equality claim. Core remains clean at `864c4e0...`; manifest, local binding and
AGENTS match; the sole review doctor result remains `24 passed, 1` bounded
legacy warning. This maintenance tranche is `FREEZE / CLOSED_BOUNDED`; no
commit or push occurred. Active continuity returns to the parked P4-C Work
Order F1 authorization rereview.
