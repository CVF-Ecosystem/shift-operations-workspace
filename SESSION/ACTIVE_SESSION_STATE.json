{
  "schema_version": "1.0",
  "repository": "shift-operations-workspace",
  "description": "Machine session state. Provider-neutral: every agent (Claude, Codex, Gemini, or any other) and every human reads this before governed work. Update it at end of session and point active_handoff at the newest handoff.",
  "mode": "cvf_enforcement_buildout",
  "active_handoff": "SESSION/handoffs/AGENT_HANDOFF_2026-07-22_PFIX6.md",
  "last_updated": "2026-07-22",
  "required_reads": [
    "SESSION/ACTIVE_SESSION_STATE.json",
    "SESSION/SESSION_MEMORY.md",
    "SESSION/handoffs/AGENT_HANDOFF_2026-07-22_PFIX6.md",
    "docs/decisions/EA_INDEPENDENT_REVIEW_2026-07-22_CODEX.md",
    "docs/implementation/EXECUTION_ROADMAP.md",
    "docs/catalog/MODULE_CATALOG.md",
    "docs/cvf/CVF_CONTROL_MAPPING.md"
  ],
  "roadmap": "docs/implementation/EXECUTION_ROADMAP.md",
  "next_allowed_move": "The P-FIX corrective tranche (P-FIX-0 through P-FIX-6) is CLOSED_BOUNDED, not 'all findings fully fixed'. P-FIX-5 previously claimed the tranche CLOSED; a SECOND independent review rejected that claim because POST /shifts/{shift_id}/close still called ledger.close_shift() directly from the router with no identity/permission/audit at all (probe: anonymous_close=200, status=CLOSED, audit_count=0), which could silently satisfy ShiftService.freeze's shift_closed prerequisite. P-FIX-6 added shift.close as a real governed action (ShiftService.close, same identity->permission->state-check->transaction(mutate+audit) shape as freeze; shift.close min role is operator in cvf_runtime/permission.py _ACTION_MIN_ROLE) and re-synced every front-door file listed in required_reads plus IMPLEMENTATION_STATUS.json and docs/catalog/MODULE_REGISTRY.json. Counting: 5 implementation tranches P-FIX-1 through P-FIX-5, plus the preparatory P-FIX-0 tranche; 6 commits before P-FIX-6; 7 P-FIX commits total after P-FIX-6's commit. P2-A remaining domains, P2-B, and P2-C may now be opened again, ONLY now that this closure surface (roadmap, control mapping, implementation status, catalog, session memory, this file, and the P-FIX-6 handoff) is fully synced - verify that sync is still true (git status clean, no other agent has since reopened P-FIX) before treating this as still valid, per the value-parked-lane reopen discipline this repository follows. Follow EXECUTION_ROADMAP.md Phase 2 in order: P2-A (replicate the CVF chain to customer requests, incidents, or handovers using the same pattern as TaskService/ShiftService - remember to route mutations through Ledger.transaction() for atomic audit and to check parent-shift-frozen status, since those are now load-bearing invariants), OR P2-B (real authentication, which should also retire the known-principals.yaml interim approval check - High Finding #4 remains unfixed: identity is header-based, data minimization is advisory only, data_scope/cost/termination have no runtime caller, refusal routing/recording is not implemented, known-principals is a registry check not real authentication), OR P2-C (frontend UI for existing verticals, keeping governance gates backend-only per FRONTEND_BACKEND_BOUNDARY.md). Do not write 'all Critical/High findings fixed' or 'all High findings fixed' anywhere - that exact overclaim pattern is what caused the P-FIX-6 rework.",
  "commit_discipline": "Commit and push to origin main as soon as each roadmap tranche is complete and gates pass. Do not batch multiple tranches into one commit. Commit message must name the tranche id and verification result.",
  "blocked_work": [
    "Reusing the labels 'enforced', '12/12', or 'golden vertical' without the callable/load-bearing/not-verified-server-side distinction from docs/cvf/CVF_CONTROL_MAPPING.md (2026-07-22 correction) - this distinction is now permanent practice, not just a P-FIX-era caveat.",
    "Claiming SqlLedger is production-verified on PostgreSQL without an actual PostgreSQL round-trip test run against a migration-created schema (the known static defect - missing tasks.version column - is fixed, but this has never been run against a live PostgreSQL instance; no Docker daemon is available in this environment). Live verification is a pre-ship gate, not required for ordinary SQLite-based development.",
    "Claiming cost/termination are load-bearing before an AI mode beyond NO_AI is wired.",
    "Claiming approval identity is fully authenticated - known-principals.yaml only rejects fabricated/inflated approver claims; it is not a signature/token/session check. Replace it (don't just supplement it) when P2-B ships.",
    "Claiming freeze is enforced for report_approved/open_handover_items_linked - those two prerequisites have no model yet and are only satisfied via an explicit, audited override, not a real check.",
    "Trusting InMemoryLedger.get_event/get_task/get_shift to return a live reference in any NEW code - they return model_copy(); do not reintroduce direct dict access that bypasses transaction() semantics.",
    "Adding a new domain vertical without wiring it through Ledger.transaction() for atomic mutation+audit, without a parent-shift-frozen check, and without persisting its evidence the way events/tasks do - these are now the established pattern, not optional extras.",
    "Adding provider-named entrypoint files (CLAUDE.md as content, etc.) - front door must stay provider-neutral (CONTRIBUTING.md).",
    "Batching more than one roadmap tranche into a single commit.",
    "Claiming 'P-FIX CLOSED' or 'all Critical/High findings fixed' without a fresh probe/test run - a prior agent made exactly this overclaim after P-FIX-5 and a second independent review rejected it (P-FIX-6, 2026-07-22: anonymous shift.close bypassed governance entirely). Use 'P-FIX CLOSED_BOUNDED' and always re-verify before repeating a closure claim from a prior handoff.",
    "Claiming High Finding #4 is fixed - P-FIX-6 did not touch approval/identity; it remains: identity header-based (not authenticated), data minimization advisory-only, data_scope/cost/termination with no runtime caller, refusal routing/recording not implemented, known-principals a registry check not real authentication.",
    "Adding a new router endpoint that mutates a governed record without first checking whether an application service (ShiftService/TaskService/EventService/CorrectionService pattern) already exists for that record type and routing through it - the P-FIX-6 gap was exactly a router calling the ledger directly, bypassing the service that would have enforced identity/permission/audit."
  ],
  "verification_snapshot": {
    "as_of": "2026-07-22",
    "pytest": "verify by running `python -m pytest -q` - do not trust a number written here, it goes stale every tranche",
    "make_validate": "PASS (catalog + session-state + file-size checks). catalog --check now recomputes metrics/Markdown and fails on drift (negative-tested).",
    "cvf_controls_callable": "12/12 (see docs/cvf/CVF_CONTROL_MAPPING.md for which are load-bearing vs. callable-only vs. not-verified-server-side)",
    "p_fix_tranche": "CLOSED_BOUNDED. P-FIX-1 freeze (cross-record, both backends), P-FIX-2 audit (atomic with mutation, both backends), P-FIX-3 evidence (persisted through SqlLedger) + approval (known-principal check, interim), P-FIX-4 migration Task.version + tightened schema parity, P-FIX-5 catalog --check (real metric/Markdown drift detection), P-FIX-6 shift.close governed (identity/permission/audit via ShiftService.close, closing the anonymous-close bypass a second independent review found after P-FIX-5's closure claim). 'Bounded' means: every Critical/High finding found by BOTH independent reviews to date has an end-to-end test; it does NOT mean no further gap can exist - do not restate this as 'all findings fixed'.",
    "known_remaining_defects": "identity is header-based, not authenticated (P2-B); data minimization is advisory only; data_scope/cost/termination have no runtime caller yet (AI-gated); refusal routing/recording per refusal-policy.yaml not implemented; known-principals is a registry check, not real authentication; PostgreSQL round-trip never run live (no Docker in this environment).",
    "second_independent_review": "docs/decisions/EA_INDEPENDENT_REVIEW_2026-07-22_CODEX.md - read before trusting any 'enforced' or 'CLOSED' claim in this repo; its recommended fix order (freeze -> atomic audit -> evidence/approval -> migration/parity -> catalog) is fixed, plus the shift.close governance gap it also implied (freeze's shift_closed check trusts close_shift) is now closed by P-FIX-6.",
    "third_review_reminder": "A prior handoff (AGENT_HANDOFF_2026-07-22_PFIX5.md) claimed the P-FIX tranche fully closed and was wrong. Do not repeat that pattern: verify with a fresh probe/test run before restating any closure claim, even one recorded in this file."
  }
}
